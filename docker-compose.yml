version: '3.8'

services:
  huntmode:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: huntmode-app
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - PORT=3000
